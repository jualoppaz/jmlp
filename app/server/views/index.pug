doctype html
head
    title(ng-bind-template='{{ title }}')
    meta(name='viewport' content='width=device-width, initial-scale=1')
    link(rel='stylesheet' href='/css/vendor.css')
    link(rel='stylesheet' href='/css/app.css')

    meta(name="version", content=version)
body
    loading(ng-if='loading')
    header(data-ui-view='header')
    main(ui-view='main' id='{{ currentState }}')
    footer#footer(data-ui-view='footer')
    script(type='text/javascript' src='/js/vendor.js')
    script(type='text/javascript' src='/i18n/angular-locale_es-es.js')
    script(type='text/javascript' src='/js/templates.js')
    script(type='text/javascript' src='/js/app.js')
    script.
        (function(i, s, o, g, r, a, m) {
            i["GoogleAnalyticsObject"] = r;
            (i[r] = i[r] || function() {
                (i[r].q = i[r].q || []).push(arguments);
            }),
            (i[r].l = 1 * new Date());
            (a = s.createElement(o)), (m = s.getElementsByTagName(o)[0]);
            a.async = 1;
            a.src = g;
            m.parentNode.insertBefore(a, m);
        })(
            window,
            document,
            "script",
            "//www.google-analytics.com/analytics.js",
            "ga"
        );
        ga("create", "UA-74931019-1", "auto");
        ga("send", "pageview");
